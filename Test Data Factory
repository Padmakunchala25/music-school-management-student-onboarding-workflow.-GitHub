// data/studentFactory.ts
import { faker } from "@faker-js/faker";

export function createStudent(type: "adult" | "child" = "adult") {
  return {
    name: faker.person.fullName(),
    email: faker.internet.email(),
    phone: faker.phone.number("+1##########"),
    type,
  };
}
